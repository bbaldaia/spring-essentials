spring:
  config:
    import: optional:classpath:.env[.properties]
  application:
    name: hero-service
  datasource:
    url: jdbc:mysql://localhost:3306/hero_service?createDatabaseIfNotExist=true
    username: ${ENV_ROOT_USER}
    password: ${ENV_ROOT_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080
  error:
    include-stacktrace: on_param
  serverlet:
    context-path: /

logging:
  level:
    bruno.spring: debug